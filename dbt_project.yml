name: 'stureplansgruppen_dbt'
version: '2.0.0'

profile: 'stureplansgruppen'

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  stureplansgruppen_dbt:
    gold:
      +materialized: view
    silver:
      +materialized: table
    silver_orders_resturang:
      enabled: false
    silver_test:
      enabled: false
